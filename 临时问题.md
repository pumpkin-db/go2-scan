# 临时问题（AI 交接用）

> 2026-08-27 写。全部已知未决 bug + 实机配置预期问题。
> 状态标注：【已核实】【用户报告·未复现】【推断·未验证】【不知道】。
> 背景全量见 `PROGRESS.md`（尤其续11-15）、架构见 `README.md`、实机方案见 `docs/实机迁移准备.md`。

---

## 一、仿真现存 bug（按严重度）

### 1. 狗腿乱飞【用户报告+历史复现，多轮修复失败】
- 现状：狗腿 = e438e07 原版（revolute 关节、零阻尼、带碰撞、无控制器），用户指定退回此版。
- 症状：腿连在身体上（不分体）但在身体附近乱甩。用户怀疑是「高度问题」（出生点 z/体高/足底接触相关），**未验证**。
- 已试过的路（全部被否，别原样再走）：
  - 腿连杆 `<kinematic>true`：腿不甩了，但走动时**分体**（腿留原地、身体传送走）——物理引擎跳过 kinematic 体，关节约束不带它们跟 base。
  - 关节全 fixed（刚体）：不分体，但**没有步态动画**，用户否决。
  - 加 `<dynamics damping=10>`：**飞得更狠**（阻尼挡住了腿被传送身体拖随，与 30Hz 步态传送互搏）。
- 关键约束：`<gazebo reference="base"><kinematic>true` **必须保留**——删掉后乱甩的腿反踢身体→雷达抖→点云全飞（实测过一次）。
- 未试的标准路：gazebo_ros_control + position controllers 连续受控（组件本机已装：
  ros-noetic-gazebo-ros-control/position-controllers/joint-state-controller），
  需要加 transmission + controller 配置；或接受 RViz RobotModel 的干净动画狗（一直正常，
  TF+ /joint_states 驱动）+ Gazebo 里用简单刚体。
- 相关文件：`scan_planner/src/simulator/Utils/go2_description/urdf/go2_description.urdf`、
  `go2_bridge/scripts/gazebo_bridge.py`（set_model_state 100Hz + set_model_configuration 30Hz 双传送）。

### 2. Depot 探索慢启动/探索很小【已核实日志证据】
- 现象：航点长期在出生点 ±2m 震荡（(-10,0)↔(-12,0)↔(-14,0)），70 秒内不离开出生区；
  历史最好的 Depot 跑（ER 53.8%）也要 ~413 秒才首次离开出生区。
  对比：indoor 成功跑前 60 秒即走出 23.4m。
- 所有 Depot 历史跑均此模式，不是今天才有。
- 嫌疑【推断】：大开阔场景前沿/效用区分度低，rl_planner 在近点间震荡；
  utility_range_factor 0.5→1.0 只缓解过一次停摆，没除根。
- 注意：rl_planner 决策本体（权重/网络）未动过；改动清单见 PROGRESS 续15
  （停滞式完成判定 / factor=1.0 / 6m 供云裁剪，全是 Depot 排障产物）。
  用户可能要求全退回 e438e07 行为——退回方法就是撤那三处。

### 3. 楼梯检测似乎无效【用户报告，现象未问清】
- 用户在 Depot 实测说「楼梯检测似乎无效」。未知是：注册表橙箭头没显示？几何检出没触发？还是根本没注意 stair_detect 开关？
- 已知事实：launch 里 `stair_detect:=true` 由 `scenes/depot/env.sh` 自动带上；
  stair_detector 注册表兜底**无条件发布**（代码层面必出 /stairs_markers 橙箭头 + /stairs_detected）；
  几何检出依赖感知高程图，而感知高程图在 Depot 楼梯区已证实不可用（报天花板 6-7m）。
- 排查入口：`/stairs_detected` 话题内容、RViz 是否开了 /stairs_markers 显示（在 Score_Stairs 组）。
- 相关：`docs/楼梯与多层探索设计.md` 五阶段计划，当前卡在 B（terrain_follow 实测）与 C（几何检出验证）。

### 4. 楼梯线阶段 B 代码已改但零验证【已核实代码状态】
- 续13 已改完：kinematic_sim 接 GT 高程（`terrain_source:=gt_file` + `elev_gt.bin`，
  感知路线的行列 bug 也修了）、楼梯注册表 entry/exit 交换（GT 点云仲裁证实原版写反，
  爬升沿 -y）、test_stair_climb.sh 三修（解析器/启动命令/记录器）。
- **从未实跑验证**（被纹理/狗腿问题打断）。预期：狗沿楼梯 z 从 0.41 爬到 ~2.9。
- 跑法：`bash simulation/test_stair_climb.sh`（一键）。

### 5. elevation_mapping 丢弃世界系点云【已核实配置矛盾，未修】
- 用户跑 indoor 见过刷屏：`received cloud frame [world] origin != sensor origin [mid360], discarding`。
- 矛盾：`simulation/elevation/mid360_robot.yaml` 期望输入是传感器系（frame=mid360），
  而仿真管线 /mid360_points 是世界系（velodyne 插件 worldFrame）。
- indoor 无害（不用高程图）；Depot 楼梯几何检测因此必然拿不到可用高程。
- 报错文案在 go2-scan 源码里搜不到，发出者未最终定位（疑似 elevation_mapping 二进制内）。
- 修法方向：要么配置改吃传感器系点云（需要一个未变换的原始话题），要么节点内改世界系兼容。

### 6. 纹理修复待用户终验【代码已改，ogre.log 部分验证】
- 已改：model.sdf 16 处 script URI 改官方 `model://Depot/materials/...` 格式；
  4 个纯 PBR visual（货箱/风扇×3）补 script 兜底；新增 2 个材质定义。
- ogre.log 证实 15 张贴图加载成功（3 张 Emissive 为经典 Gazebo 不支持，不影响外观）。
- 用户尚未确认视觉效果，此后注意力被狗腿问题带走。
- 若仍白：对照 `~/.gazebo/models/brick_box_3x1x3`（官方标准样本）再查。

### 7. TARE 封存状态【已核实】
- CMU terrainAnalysis/Ext 段错误（两次）+ /state_estimation 饿死 + NaN 目标 length_error，
  run3 出一个有效对照（ER 38.3%）后封存，资源不再投入。用户若要求复活看 PROGRESS 续5-9。

### 8. 其他已知小事
- spawn 竞态：首启 "model does not exist" 等几秒重试若报 "entity already exists" = 实际成功（无害）。
- 工作区有未提交改动：纹理修复、launch 增补（live_score/stair_detect/terrain 参数）、
  test_stair_climb.sh、scene.yaml、env.sh——git status 可见。
- UFEP 冻结：编译产物在 ~/claude/raicom/ufep_ws，不再投入（用户指示）。

---

## 二、实机配置预期问题（迁移前必读）

> 完整方案在 `docs/实机迁移准备.md`，这里只列「会咬人的坑」。

### 感知/定位
1. **Point-LIO 话题名不知道**：本机 `~/claude/SLAM_LIO/point_lio_ws` 未实跑，
   输出是否叫 /Odometry + /cloud_registered 待核实（FAST-LIO2 是确认的）。
2. **IMU 接入不知道**：FAST-LIO2/Point-LIO 都要 IMU，Go2 EDU 的 IMU 从哪个话题拿
   （unitree_sdk2 还是狗内建话题）——不知道，查 Go2 EDU 资料。
3. **时间同步**：MID360 与 IMU/系统时间同步（PTP？）没做过，FAST-LIO 系对此敏感。不知道现状。
4. **雷达驱动二选一**：livox_ros_driver（point_lio_ws 带）/ livox_ros_driver2（参考工程带），未选型。
5. **body→lidar 外参是仿真值**：`grid_map.cpp:57-67` 硬编码 (0.2, 0, 0.2077)，实机必须实测重填，
   否则 raycast 原点系统性偏移。
6. **赋色链路不存在**：比赛产物是彩色点云，仓库无任何赋色实现，选型未做。
7. **D435i 相机**：JetPack 6 下不工作（已核实）→ 系统必须锁 JetPack 5.1.1/Ubuntu 20.04/ROS1 Noetic；
   相机与雷达外参未标定。

### 运动控制
8. **/cmd_vel → SDK2 桥不存在**：链路止于 closed_loop_controller 的 /cmd_vel，
   之后要自研桥节点（订 /cmd_vel → unitree_sdk2 DDS → 狗）。
9. **SDK2 运动模式 API 细节不知道**：速度指令形态/频率上限/延迟都没实测；
   `~/unitree/unitree_sdk2` 有 example/go2 可起步。
10. **CycloneDDS 共存未实测**：SDK2 要求 CycloneDDS 0.10.2；ROS1 不用 DDS 理论无冲突，没跑过。
    （注意：与 ROS2 Humble 有已知冲突，别在狗上装 Humble。）
11. **安全层完全不存在**：看门狗/限速/地形禁行全要做。摔倒=取消资格是硬约束，
    实机第一步就是安全层，不是探索。

### 环境/部署
12. **硬编码 $HOME 路径**：launch/脚本大量 `~/claude/raicom/go2-scan`，
    实机用户名不同要全改或参数化。
13. **conda 污染**：启动脚本已自动清理 PATH 里的 conda，但新写的节点脚本如果直接
    `python3` 可能掉进 miniconda（无 numpy/无 ROS ABI）——统一用 `/usr/bin/python3` 或显式 env。
14. **Orin NX 16GB 内存上限**：SLAM+规划+可视化同跑负载未实测；VLM 只能 Int4 低频。
15. **仿真专用节点别带上实机**：go2_kinematic_sim、gazebo_bridge、map_pub、
    scan_cloud_accumulator 的 GT 依赖——实机 launch 需要一份裁剪版（还没有）。
16. **点云坐标系分工**：实机会有两套云——雷达原始（传感器系，/livox/lidar）与
    SLAM 配准（世界系，/cloud_registered）。ARiADNE 链的 cloud_range_filter 现在只吃世界系；
    elevation_mapping 配置期望传感器系。接线时别混。

---

## 三、纪律提醒（血泪教训，照做）

1. 起仿真前 `bash simulation/kill_all_sim.sh` + `pgrep -x gzserver` 复核为空（孤儿 gzserver
   会污染新栈：陈旧 /clock + 陈旧点云，曾致两小时误诊）。
2. 不用 `timeout` 包 launch 脚本（只杀外层，gzserver 孤儿化）。
3. 记录器用 `PYTHONUNBUFFERED=1`（rostopic echo 是 Python，杀进程丢缓冲）。
4. 引用第三方/能力必须区分「已核实」vs「推断」——本项目最大的坑就是把搜到的名字当事实。
5. 改 URDF 前先 `git diff` 存档；狗模型相关改动历史全在 PROGRESS 续14-15。
6. 用户终版参数不许动：ARiADNE 收发量程 6m（res 0.2、z 带 [0.2,0.8]）。
